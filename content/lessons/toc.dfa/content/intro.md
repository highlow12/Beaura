# 결정적인 전이

결정적 유한 오토마톤(DFA)은 모든 상태와 입력 기호의 쌍에 대해 다음 상태가 정확히 하나인 FSM입니다. 따라서 같은 시작 상태와 같은 입력은 언제나 같은 실행 경로와 같은 결과를 만듭니다.

DFA는 보통 `(Q, Σ, δ, q₀, F)`로 나타내며 δ의 형식은 `Q × Σ → Q`입니다. 입력을 모두 읽은 뒤 현재 상태가 F에 들어 있으면 문자열을 수용하고, 그렇지 않으면 거부합니다.
