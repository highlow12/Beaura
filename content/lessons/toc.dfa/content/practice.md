## 짝수 개의 1을 인식하기

짝수 개의 `1`을 갖는 언어는 `even`과 `odd` 두 상태로 인식할 수 있습니다. `0`을 읽으면 현재 상태를 유지하고, `1`을 읽으면 두 상태 사이를 전환합니다. 시작 상태이자 수용 상태를 `even`으로 두면 빈 문자열과 1이 두 개인 문자열을 수용합니다.

DFA 문제에서는 전이 규칙과 수용 상태를 따로 확인하세요. 동일한 전이표라도 F를 바꾸면 인식하는 언어가 달라지며, 시작 상태를 잘못 잡으면 첫 기호를 읽기 전부터 결과가 달라집니다.
