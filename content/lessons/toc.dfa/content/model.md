# 수용 여부 추적

DFA 실행은 현재 상태를 하나만 유지하면서 반복할 수 있습니다. 처음에는 q₀에서 시작하고, 입력 문자열의 각 기호 `a`에 대해 `state = δ(state, a)`로 갱신한 뒤 마지막 상태를 수용 상태 집합과 비교합니다.

완전한 DFA는 모든 입력에 전이가 있으므로 어떤 문자열에도 실행이 멈추지 않습니다. 특정 입력을 허용하지 않으려면 거부 상태(sink state)를 두고 그 상태에서 모든 기호를 다시 거부 상태로 보내는 방식이 흔합니다.
