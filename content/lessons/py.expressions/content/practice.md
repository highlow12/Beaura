## 표현식을 조합해 읽기

작은 표현식을 괄호와 연산자로 결합하면 더 큰 표현식이 됩니다. total = price * count + shipping은 곱셈 결과와 배송비를 더한 뒤 그 결과를 저장합니다.

먼저 각 이름이 어떤 값을 가리키는지 확인하고, 안쪽 계산부터 결과를 적어 보세요. 이렇게 중간 결과를 추적하면 긴 코드도 단계별로 검증할 수 있습니다.
