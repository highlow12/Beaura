## 표현식은 결과를 만든다

표현식은 평가될 때 결과값 하나를 만듭니다. score + bonus는 두 이름이 가리키는 값을 읽고 합을 계산한 뒤 새로운 결과를 내놓습니다. 이름만 적은 score도 현재 값을 읽는 간단한 표현식입니다.

반면 score = 10은 값을 계산해 이름에 연결하는 할당문입니다. 표현식과 문장을 구분하면 어느 부분이 값을 만들고 어느 부분이 프로그램의 동작을 진행하는지 읽기 쉬워집니다.
