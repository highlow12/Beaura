## 작동 방식

두 스레드가 같은 값을 동시에 읽고 각각 1을 더한 뒤 쓰면 둘 다 같은 결과를 저장할 수 있습니다. 락·원자 명령·메시지 전달로 임계 구역을 보호합니다.

### 예시로 확인하기

초기값 0에서 A와 B가 각각 증가하면 기대값은 2지만, 둘이 0을 읽은 뒤 1을 쓰면 최종값은 1입니다. 실행 타이밍에 따라 버그가 간헐적으로 나타납니다.

### 설계에서의 선택

임계 구역을 넓게 잠그면 안전하지만 병렬성이 줄어듭니다. 보호 범위를 작게 만들되 불변식이 깨지는 모든 접근을 빠짐없이 포함해야 합니다.
