# 값과 참조

값 의미를 쓰는 변수는 대입할 때 값 자체가 복사되는 것처럼 관찰되고, 참조 의미를 쓰는 변수는 같은 객체를 가리킬 수 있습니다. 언어가 정확히 복사하는지 참조를 공유하는지는 타입과 대입 규칙으로 정해지므로 “스택 변수 = 값, 힙 변수 = 참조”라고 단정하면 안 됩니다.

함수 호출에서는 인자 전달 방식도 중요합니다. 객체 참조를 값으로 전달하는 언어에서는 참조 자체는 복사되지만 가리키는 객체의 내부 변경은 호출자에게 보일 수 있습니다. 변수 재대입과 객체 변이를 구분해 관찰해야 합니다.
