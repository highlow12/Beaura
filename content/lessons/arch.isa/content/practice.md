## 명령어 필드 해석하기

**ISA(명령어 집합 구조)**는 소프트웨어가 사용할 수 있는 명령과 레지스터, 데이터 형식 등의 약속입니다. 명령어의 **opcode**는 수행할 연산을, **operand**는 연산에 사용할 레지스터·즉시값·주소 등을 지정합니다.

예를 들어 `ADD R1, R2`에서 ADD는 덧셈을 지시하는 opcode이고 R1과 R2는 operand입니다. 같은 비트 패턴도 ISA가 다르면 필드 경계나 뜻이 달라질 수 있으므로, 명령어는 해당 ISA의 인코딩 규칙과 함께 해석해야 합니다.
