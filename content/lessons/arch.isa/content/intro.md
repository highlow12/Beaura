# 명령어와 ISA

소스 코드의 덧셈 한 줄이 CPU가 이해하는 비트 패턴으로 바뀌려면 명령어 형식이 약속되어 있어야 합니다.

ISA(Instruction Set Architecture)는 명령어 종류, 레지스터, 주소 지정 방식, 메모리 모델 등 프로그램이 볼 수 있는 CPU 계약입니다. 한 명령어는 보통 opcode와 operand 정보를 포함합니다.

이 레슨의 목표는 용어를 외우는 데 그치지 않고, 입력이 어떻게 표현되고 어떤 하드웨어 경로를 거쳐 결과가 되는지 설명하는 것입니다.
