# 정점이 픽셀이 되기까지

그래픽 파이프라인은 장면의 입력 데이터를 화면에 표시할 색으로 바꾸는 여러 단계의 연속입니다. 보통 정점 셰이더가 정점 위치를 변환하고, primitive assembly가 정점들을 삼각형 같은 도형으로 묶은 뒤, 래스터화가 도형이 덮는 화면 위치를 fragment 후보로 나눕니다.

fragment 셰이더는 각 후보 위치의 색과 깊이 같은 값을 계산하고, 마지막에 프레임버퍼에 기록된 색이 화면에 표시됩니다. 단계가 나뉘어 있어 GPU는 서로 다른 정점과 fragment를 동시에 처리할 수 있습니다.
