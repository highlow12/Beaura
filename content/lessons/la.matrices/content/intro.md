# 숫자를 직사각형으로 배열하기

행렬(matrix)은 수를 행(row)과 열(column)의 격자로 배열한 것입니다. m×n 행렬은 m개의 행과 n개의 열을 가지며, 원소 Aᵢⱼ는 i번째 행과 j번째 열에 놓입니다.

행렬의 크기(shape)는 연산 가능 여부를 결정합니다. 벡터를 열벡터로 쓰면 행렬이 좌표를 다른 좌표로 보내는 함수처럼 작동할 수 있습니다.
