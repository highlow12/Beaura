# 프로그램의 모양과 뜻

문법(syntax)은 프로그램을 어떤 기호 순서로 써야 하는지를 정합니다. 의미(semantics)는 문법에 맞는 프로그램이 실행될 때 어떤 값과 상태 변화를 만드는지를 정합니다. 같은 의미를 다른 문법으로 표현할 수 있고, 문법이 맞아도 의미상 오류가 있을 수 있습니다.

예를 들어 `x + 1`은 올바른 산술식 모양이지만 x가 숫자라는 환경이 없으면 실행 의미를 정할 수 없습니다. 컴파일러는 두 질문을 분리해 앞에서는 구조를, 뒤에서는 이름·타입·실행 규칙을 검사합니다.
