# 선형 보간과 곡선

선형 보간은 시작값 `a`와 끝값 `b` 사이를 `a + (b - a) × t`로 계산합니다. `t = 0`이면 `a`, `t = 1`이면 `b`, `t = 0.5`이면 두 값의 중간입니다.

선형 보간은 속도가 일정해 움직임이 기계적으로 느껴질 수 있습니다. 이징 곡선은 시간 비율을 다른 곡선에 통과시켜 시작과 끝에서 천천히 움직이거나 중간에서 가속하도록 만듭니다. 공간의 경로와 시간에 따른 속도 변화는 서로 구분해 생각해야 합니다.
