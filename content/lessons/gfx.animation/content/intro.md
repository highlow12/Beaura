# 키프레임과 시간

애니메이션은 모든 프레임의 값을 직접 저장하기보다 중요한 시점의 상태인 키프레임을 기록하고 그 사이를 계산하는 경우가 많습니다. 각 키프레임에는 시간과 위치·회전·크기 같은 속성 값이 들어갑니다.

현재 시간이 두 키프레임 사이에 있으면 먼저 구간 안의 비율 `t`를 구합니다. 시작 시간이 `t0`, 끝 시간이 `t1`, 현재 시간이 `time`이면 `t = (time - t0) / (t1 - t0)`입니다. 구간 안에서 `t`는 보통 0에서 1 사이입니다.
