# 규칙으로 문자열 생성하기

형식 문법은 비단말 기호를 생산 규칙으로 바꾸어 문자열을 생성합니다. 문법은 보통 비단말 집합, 단말 집합, 생성 규칙, 시작 기호로 구성하며, 생성이 끝나 모든 기호가 단말만 남으면 문장이 됩니다.

예를 들어 `S → aS | b`에서 시작 기호 S를 계속 바꾸면 `b`, `ab`, `aab`처럼 문자열을 만들 수 있습니다. 규칙을 어떤 순서로 적용했는지가 유도(derivation)이며, 같은 문자열에 여러 유도가 있을 수도 있습니다.
