# 클로저의 두 부분

클로저(closure)는 함수 코드와 그 함수가 정의될 때 캡처한 렉시컬 환경의 쌍입니다. 따라서 `make-adder(2)`가 반환한 함수는 호출이 끝난 뒤에도 `2`라는 바깥 값을 기억해 다른 인자에 더할 수 있습니다.

캡처한 변수가 변경 가능한지, 캡처 시점에 값을 복사하는지 위치를 공유하는지는 언어마다 다릅니다. 반복문 안에서 만든 함수가 마지막 값만 보는 문제처럼, 환경을 언제 어떻게 캡처하는지 확인해야 합니다.
