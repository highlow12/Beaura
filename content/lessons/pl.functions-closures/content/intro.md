# 함수를 값으로 다루기

일급 함수(first-class function)는 함수를 변수에 저장하고, 인자로 전달하고, 결과로 반환할 수 있는 값처럼 취급합니다. 고차 함수는 함수를 인자로 받거나 함수를 반환해 반복되는 제어 구조를 값의 조합으로 표현합니다.

함수 호출은 코드 위치만으로 끝나지 않고 매개변수와 지역 환경을 새로 만듭니다. 함수가 반환된 뒤에도 안쪽 함수가 바깥 지역 변수를 참조해야 한다면 그 환경을 보존하는 표현이 필요합니다.
