# MTU와 단편화

링크마다 한 번에 운반할 수 있는 프레임 크기가 다르므로, 너무 큰 IP 패킷을 그대로 다음 링크에 실을 수 없을 수 있습니다.

MTU(Maximum Transmission Unit)는 링크가 한 프레임에 실을 수 있는 IP 패킷 전체(IP 헤더와 데이터 포함)의 최대 크기입니다. 경로의 가장 작은 MTU가 종단 간 전송 크기를 제한합니다.

이 레슨에서는 패킷이 어느 계층에서 어떤 주소와 상태를 얻는지 따라가며 네트워크 용어를 구분합니다.
