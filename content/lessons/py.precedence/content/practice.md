## 손으로 추적하는 방법

복잡한 식은 한 번에 외우지 말고 가장 안쪽 괄호부터 지웁니다. 그다음 같은 우선순위의 연산자를 처리하고, 중간 결과를 새 값으로 적습니다. 대부분은 왼쪽에서 오른쪽으로 묶지만, 거듭제곱 `**`는 오른쪽에서 왼쪽으로 묶입니다. 예를 들어 `2 ** 3 ** 2`는 `2 ** (3 ** 2)`로 계산합니다.

팀 코드에서는 우선순위를 알고 있더라도 괄호를 사용해 계산 의도를 명시하면 읽는 사람의 실수를 줄일 수 있습니다.
