# 순서를 지운 선택

조합은 n개의 서로 다른 대상 중 r개를 순서 없이 선택하는 경우의 수입니다. {A,B}와 {B,A}를 같은 선택으로 보므로 순열보다 경우의 수가 작습니다.

조합 수는 nCr 또는 C(n,r)로 쓰며 nCr = n!/(r!(n−r)!)입니다. r개를 고르는 것은 고르지 않을 n−r개를 정하는 것과 같아서 nCr=nC(n−r)라는 대칭도 있습니다.
