# ARP와 로컬 주소 찾기

호스트는 목적지 IP를 알아도 이더넷 프레임을 만들려면 같은 링크에서 사용할 MAC 주소가 필요합니다.

ARP는 로컬 IPv4 네트워크에서 IP 주소에 대응하는 MAC 주소를 질의하는 프로토콜입니다. 결과는 ARP 캐시에 일정 시간 저장되어 반복 브로드캐스트를 줄입니다.

이 레슨에서는 패킷이 어느 계층에서 어떤 주소와 상태를 얻는지 따라가며 네트워크 용어를 구분합니다.
