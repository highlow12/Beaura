## 작동 방식

상위 계층의 데이터에 하위 계층이 헤더를 붙여 캡슐화합니다. 수신 측은 각 계층에서 자신의 헤더를 해석하고 제거해 다음 계층에 payload를 넘깁니다.

### 예시로 확인하기

웹 요청은 HTTP 메시지이고, TCP 세그먼트에 실린 뒤 IP 패킷과 링크 프레임으로 감싸집니다. 각 계층은 아래 계층의 전달 방식만 알면 됩니다.

### 설계에서의 선택

계층화는 구현 교체와 문제 격리를 쉽게 하지만 헤더와 변환 비용이 늘어납니다. 한 계층의 책임을 다른 계층에 중복 구현하면 디버깅과 호환성이 나빠집니다.
