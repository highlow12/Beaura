# 반복을 줄이는 테이블 설계

정규화는 함수적 종속을 근거로 데이터를 여러 relation으로 분해해 중복과 삽입·삭제·갱신 이상을 줄이는 과정입니다. 1NF는 각 칸을 원자적인 값으로 두고 반복 그룹을 제거하는 출발점입니다.

2NF는 복합 키의 일부에만 종속되는 비키 속성을 분리하고, 3NF는 비키 속성이 다른 비키 속성을 결정하는 이행 종속을 줄입니다. 정규형 숫자를 높이는 것 자체가 목적이 아니라, 변경되는 사실을 한 곳에서 관리하고 JOIN으로 다시 조합할 수 있게 만드는 것이 목적입니다.
