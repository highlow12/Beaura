## 작동 방식

양수는 일반 이진수로 쓰고 남는 앞자리를 0으로 채웁니다. 음수는 절댓값을 n비트로 쓴 다음 모든 비트를 반전하고 1을 더합니다.

### 예시로 확인하기

4비트에서 +3은 `0011`입니다. +3의 비트를 반전한 `1100`에 1을 더하면 `1101`이 되고, 이는 -3을 뜻합니다.

### 설계에서의 선택

2의 보수는 0이 하나뿐이고 같은 덧셈 회로로 양수와 음수를 처리할 수 있습니다. 대신 표현 범위가 비대칭이라 -8은 표현되지만 +8은 4비트에서 표현되지 않습니다.
