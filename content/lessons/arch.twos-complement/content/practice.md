## 4비트 음수 만들기

**MSB(most significant bit)**는 가장 왼쪽의 최상위 비트입니다. 2의 보수에서 MSB가 1이면 음수로 해석합니다. n비트 **범위(range)**는 -2^(n-1)부터 2^(n-1)-1이며, 이 범위를 벗어나는 계산이 **오버플로(overflow)**입니다.

4비트로 -3을 만들려면 +3인 `0011`의 모든 비트를 뒤집어 `1100`을 만들고 1을 더해 `1101`을 얻습니다. 확인할 때 `1101`에 같은 과정을 적용하면 `0011`, 즉 3이 됩니다.
