# 가까이 다가가는 값

x가 a와 같아지는 순간의 값만 보는 것이 아니라, x가 a에 한없이 가까워질 때 f(x)가 다가가는 값을 극한 limₓ→ₐ f(x)로 나타냅니다. x=a에서 함수가 정의되지 않아도 극한은 존재할 수 있습니다.

좌극한은 왼쪽에서, 우극한은 오른쪽에서 접근한 값입니다. 두 값이 같아야 양쪽 극한이 존재하며, 함수값 f(a)와 극한값은 서로 다를 수 있습니다.
