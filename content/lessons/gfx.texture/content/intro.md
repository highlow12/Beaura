# 이미지를 표면에 붙이기

텍스처는 물체 표면에 사용할 이미지나 수치 데이터를 저장한 리소스입니다. UV 좌표는 정점이 텍스처의 어느 위치를 읽을지 나타내며, 보통 `u`와 `v`를 0 ~ 1 범위로 정규화해 이미지의 왼쪽 아래부터 오른쪽 위 같은 규칙으로 해석합니다.

fragment 셰이더는 보간된 UV로 텍스처를 샘플링합니다. 샘플 위치가 픽셀 격자와 정확히 맞지 않으면 주변 texel을 어떻게 섞을지 결정해야 하며, nearest는 하나를 고르고 linear는 주변 값을 섞는 대표적인 필터입니다.
