## CS DUO에서의 비유

브라우저 앱이 Dexie를 통해 데이터를 읽고 쓰는 경우 Dexie가 DBMS API에 가까운 역할을 하고, 실제 브라우저 저장소가 데이터베이스에 해당합니다. 앱의 화면은 질의 결과를 보여 주는 소비자이지 저장 구조 그 자체가 아닙니다.

좋은 데이터베이스 설계는 “어디에 값이 저장되는가”만 정하지 않습니다. 어떤 질의를 지원해야 하는지, 동시에 변경될 때 어떤 규칙을 지켜야 하는지, 장애 뒤에도 어떤 상태를 복구해야 하는지를 함께 명시합니다.
