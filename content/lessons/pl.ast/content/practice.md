## AST 순회

표현식 값을 계산하는 방문자는 리터럴에서 값을 얻고, 연산 노드에서는 자식 노드를 재귀적으로 평가한 뒤 연산을 적용합니다. 같은 AST를 타입 검사 방문자, pretty-printer, 코드 생성기가 각자의 목적에 맞게 순회할 수 있습니다.

AST를 수정하는 최적화는 원래 프로그램과 관찰 가능한 결과를 보존해야 합니다. 부수 효과가 있는 함수 호출을 단순히 상수로 접거나 자식 순서를 바꾸면 트리 모양은 간단해져도 의미가 달라질 수 있습니다.
