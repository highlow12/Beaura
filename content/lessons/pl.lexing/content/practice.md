## 렉서 오류와 파서 오류

문자열 리터럴의 닫는 따옴표가 없거나 어떤 토큰 패턴에도 맞지 않는 문자가 나오면 렉서가 오류를 보고합니다. 토큰은 만들어졌지만 순서가 문법에 맞지 않으면 그다음 파서의 오류입니다.

토큰화를 손으로 할 때는 왼쪽에서 오른쪽으로 커서를 이동하고, 각 위치에서 가장 긴 유효 토큰을 표시하세요. 토큰 값과 원문 위치를 함께 적으면 `123`이 숫자 하나인지 `1`, `2`, `3`인지 같은 경계 판단을 검증할 수 있습니다.
