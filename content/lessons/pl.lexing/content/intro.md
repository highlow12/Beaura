# 문자를 토큰으로

렉서(lexer)는 소스 코드의 문자 스트림을 식별자, 숫자, 연산자, 키워드 같은 토큰의 순서로 바꿉니다. 파서는 토큰의 경계를 다시 찾을 필요 없이 문법 구조에 집중할 수 있습니다.

토큰에는 보통 종류와 원문 또는 값, 소스 위치가 들어갑니다. 위치 정보는 오류 메시지에서 사용되므로 공백과 주석을 버리더라도 시작 줄·열을 추적해 두는 것이 중요합니다.
