# 패턴 우선순위

렉서는 현재 위치에서 일치하는 토큰 후보를 찾고, 많은 언어에서 가장 긴 일치(longest match)를 우선합니다. `=`와 `==`가 함께 있다면 두 글자를 읽을 수 있는 경우 `==`를 먼저 선택해야 토큰이 잘못 쪼개지지 않습니다.

키워드와 일반 식별자가 같은 문자 패턴을 공유할 때는 식별자를 읽은 뒤 예약어 표에서 다시 확인하는 방식이 흔합니다. 주석과 공백은 토큰으로 내보내지 않을 수 있지만 문자열 리터럴 안의 공백은 보존해야 합니다.
