# 기호에서 언어까지

형식 언어는 의미보다 모양과 규칙을 먼저 다루는 문자열의 집합입니다. 알파벳(Σ)은 사용할 수 있는 기호의 유한한 집합이고, 문자열은 그 기호를 유한 번 이어 붙인 결과입니다. 아무 기호도 쓰지 않은 문자열은 빈 문자열 ε로 씁니다.

언어는 같은 알파벳으로 만들 수 있는 문자열 가운데 규칙에 맞는 것만 모은 집합입니다. 따라서 `101`이라는 문자열과 `101`을 포함할지 결정하는 언어는 서로 다른 대상이며, 이 구분이 오토마톤과 문법을 읽는 출발점입니다.
