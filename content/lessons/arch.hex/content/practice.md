## 4비트씩 바꾸기

16진수 한 자리는 이진수 4비트인 **니블(nibble)** 하나와 정확히 대응합니다. 니블 두 개는 8비트인 **바이트(byte)** 하나입니다. `0x`는 뒤의 수가 16진수라는 접두사입니다.

예를 들어 `1010 0111`을 오른쪽부터 네 비트씩 나누면 `1010`은 A, `0111`은 7이므로 `0xA7`입니다. 반대로 `0x3C`는 `0011 1100`입니다. 앞의 0은 비트 수를 맞추기 위한 것이며 값은 바꾸지 않습니다.
