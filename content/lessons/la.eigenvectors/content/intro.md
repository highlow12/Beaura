# 변환에도 방향이 남는 벡터

행렬 A의 고유벡터 v는 0이 아닌 벡터로서 Av=λv를 만족합니다. 변환 결과는 언제나 v의 스칼라배입니다. λ>0이면 같은 방향, λ<0이면 반대 방향으로 놓이고, λ=0이면 영벡터가 됩니다.

λ를 고유값이라고 합니다. 하나의 고유값에 여러 고유벡터가 대응할 수 있으며, 영벡터는 어떤 λ에도 식을 만족하므로 고유벡터에서 제외합니다.
