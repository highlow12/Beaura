## 재대입과 현재 값 추적

변수 이름은 한 번 정한 값을 영원히 보관하는 상자가 아닙니다. 같은 이름에 다시 대입하면 그 뒤부터는 새 값을 가리킵니다.

~~~python
score = 10
score = score + 5
score = score * 2
~~~

각 대입문은 오른쪽을 **현재 값으로 먼저 계산한 뒤** 결과를 왼쪽 이름에 연결합니다. 따라서 위 코드는 `10 → 15 → 30` 순서로 `score`가 변합니다. 여러 줄의 코드를 읽을 때는 변수의 값을 줄마다 갱신하며 추적하세요.
