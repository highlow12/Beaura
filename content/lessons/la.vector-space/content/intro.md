# 공간을 만드는 기준 벡터

벡터 공간의 기저(basis)는 공간의 모든 벡터를 만들 수 있으면서 서로 선형 결합으로 중복되지 않는 벡터들의 집합입니다. 평면의 표준 기저 (1,0),(0,1)로 모든 (x,y)를 만들 수 있습니다.

기저 벡터의 개수는 그 공간의 차원(dimension)입니다. 같은 공간에는 여러 기저가 있을 수 있지만, 기저에 포함된 벡터 수는 변하지 않습니다.
