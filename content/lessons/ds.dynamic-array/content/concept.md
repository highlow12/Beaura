## 재할당과 복사

capacity가 가득 차면 더 큰 공간을 새로 확보하고 기존 원소를 복사한 뒤 새 값을 추가합니다. 이 한 번의 append는 많은 원소를 옮겨야 하므로 O(n) 비용이 될 수 있습니다.

하지만 용량을 일정 비율로 늘리면 매 append가 재할당되는 것은 아닙니다. 여러 번의 append를 묶어 평균하면 append의 amortized 비용은 O(1)로 설명할 수 있습니다.
