# 소켓과 클라이언트·서버

응용 프로그램은 패킷 헤더를 직접 조립하기보다 운영체제가 제공하는 소켓으로 네트워크 연결을 파일처럼 다룹니다.

소켓은 프로세스가 네트워크로 데이터를 보내고 받는 추상적인 통신 종단점입니다. 서버는 주소에 bind·listen·accept하고, 클라이언트는 connect한 뒤 send·receive를 사용합니다.

이 레슨에서는 패킷이 어느 계층에서 어떤 주소와 상태를 얻는지 따라가며 네트워크 용어를 구분합니다.
