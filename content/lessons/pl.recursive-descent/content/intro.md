# 문법을 함수 호출로

재귀 하강 파서는 각 비단말에 대응하는 함수를 두고 현재 토큰을 보며 하위 규칙을 호출하는 하향식 파서입니다. `parseExpression`이 `parseTerm`을 부르는 식으로 문법의 계층이 코드 구조에 드러납니다.

파서는 토큰을 소비할 때 예상한 종류인지 확인하고, 맞지 않으면 현재 위치와 기대 토큰을 포함한 오류를 반환해야 합니다. 성공한 함수는 자신이 담당한 문법 조각의 AST 노드를 돌려줍니다.
