## 표현식 파서 흐름

표현식 파서는 보통 낮은 우선순위의 `expression`, 그 안의 `term`, 그 안의 `factor` 순으로 함수를 나눕니다. `factor`가 숫자나 괄호식을 처리하고, 상위 함수가 `*`와 `+` 반복을 조합하면 곱셈이 덧셈보다 깊은 트리가 됩니다.

오류 복구가 필요하면 동기화 지점인 세미콜론이나 줄 끝까지 토큰을 건너뛸 수 있지만, 교육용 파서는 첫 오류에서 명확히 멈추는 편이 원인 추적에 좋습니다. 어느 토큰을 소비했는지 불변식으로 정해 두세요.
