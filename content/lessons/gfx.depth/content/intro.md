# 앞에 있는 표면 고르기

한 화면 위치에 여러 삼각형이 겹치면 모든 fragment를 그대로 표시할 수 없습니다. 깊이 버퍼(depth buffer)는 각 픽셀에 지금까지 통과한 fragment의 깊이를 저장하고, 새 fragment의 깊이와 비교해 카메라에 더 가까운 표면을 선택합니다.

일반적인 깊이 테스트는 새 깊이가 저장된 깊이보다 작을 때 통과시키고, 통과한 경우 색과 깊이를 함께 갱신합니다. 프레임을 시작할 때 깊이 버퍼를 far 쪽 값으로 지워야 첫 fragment가 올바르게 비교됩니다.
