# 세 주소 코드와 SSA

세 주소 코드에서는 한 명령이 보통 두 피연산자를 계산해 하나의 임시 변수에 저장합니다. `t1 = b * c`, `t2 = a + t1`처럼 복잡한 식을 작은 명령으로 쪼개면 값의 의존성을 추적하기 쉽습니다.

SSA(static single assignment)는 각 가상 레지스터에 한 번만 대입하도록 하며, 분기에서 합쳐지는 값은 φ 함수로 표현합니다. 한 번 대입이라는 규칙은 정의-사용 관계와 상수 전파를 단순하게 만듭니다.
