# 프런트엔드와 백엔드 사이

중간 표현(IR)은 소스 언어의 AST와 특정 CPU 명령어 사이에 놓이는 공통 표현입니다. 여러 소스 언어를 하나의 백엔드로 보내거나 여러 CPU를 하나의 프런트엔드로 지원할 때 변환 경계를 분리해 줍니다.

좋은 IR은 최적화에 필요한 값의 흐름과 제어 흐름을 명시하면서도 대상 기계의 세부사항을 지나치게 고정하지 않습니다. IR의 선택은 컴파일러가 어떤 분석과 변환을 쉽게 할지 결정합니다.
