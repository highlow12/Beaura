## 비트의 위치마다 값이 다르다

이진수의 각 자리는 오른쪽부터 1, 2, 4, 8, 16처럼 2의 거듭제곱 값을 가집니다. 비트가 1인 자리의 값을 더하면 이진수를 십진수로 바꿀 수 있습니다.

예를 들어 `10110₂`는 `16 + 4 + 2 = 22`입니다. 반대로 십진수를 이진수로 바꿀 때는 어떤 2의 거듭제곱들을 더해 그 값을 만들 수 있는지 생각하면 됩니다.

비트 연산이나 메모리 주소를 이해할 때도 이 위치값이 계속 등장하므로, 단순 암기보다 각 비트가 얼마를 나타내는지 바로 읽는 연습이 중요합니다.
