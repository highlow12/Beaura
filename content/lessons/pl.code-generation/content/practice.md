## 생성 결과 검증

작은 IR 조각을 명령어로 바꿀 때는 각 IR 임시값이 언제 정의되고 언제 마지막으로 사용되는지 표시하세요. 마지막 사용 뒤에는 레지스터를 재사용할 수 있고, spill을 넣었다면 load·store가 모든 경로에서 올바른 값을 읽는지도 확인해야 합니다.

기계어를 직접 쓰지 않는 VM 대상 코드 생성도 같은 원리를 따릅니다. 대상이 네이티브 ISA인지 바이트코드인지와 무관하게 값의 수명, 호출 경계, 제어 흐름을 명시적으로 보존해야 합니다.
