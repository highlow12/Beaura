# IR에서 명령어로

코드 생성기는 IR 연산을 대상 ISA의 명령어열로 선택하고, 각 값에 레지스터나 스택 위치를 배정합니다. 같은 덧셈도 대상 CPU의 레지스터 수, 즉시값 범위, 주소 지정 방식에 따라 서로 다른 명령어 조합이 됩니다.

코드 생성 결과는 원래 프로그램의 관찰 가능한 의미를 보존해야 합니다. 계산 결과뿐 아니라 함수 호출 규약, 예외, 메모리 접근 순서, 반환 레지스터 같은 ABI 약속도 지켜야 실행 코드가 서로 연결됩니다.
