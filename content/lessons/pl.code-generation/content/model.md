# 레지스터 배정과 호출 규약

레지스터 배정은 동시에 살아 있는 값들을 제한된 물리 레지스터에 매핑하는 문제입니다. 레지스터가 부족하면 일부 값을 메모리로 spill하고 나중에 다시 load하며, 이 비용이 실행 시간에 반영됩니다.

호출 규약은 인자와 반환값을 어느 레지스터·스택에 두는지, 어떤 레지스터를 호출자가 보존하는지, 스택 프레임을 어떻게 정리하는지를 합의합니다. 호출자와 피호출자 중 한쪽만 규약을 지키면 간단한 코드도 반환 뒤 상태가 깨집니다.
