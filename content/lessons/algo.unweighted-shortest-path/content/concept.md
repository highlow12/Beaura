## 거리와 predecessor

시작점의 거리를 0으로 두고, 현재 정점의 이웃을 처음 발견할 때 distance[next] = distance[current] + 1로 기록합니다. 동시에 predecessor를 저장하면 나중에 목표에서 시작점으로 경로를 되짚을 수 있습니다.

이미 거리가 기록된 정점은 더 짧은 경로로 다시 발견될 수 없으므로 큐에 다시 넣지 않습니다. 인접 리스트를 사용하면 시간 복잡도는 O(V + E)입니다.
