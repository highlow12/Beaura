# 값만으로 기울기 추정하기

함수식을 모르거나 복잡한 시뮬레이션 결과만 있을 때는 가까운 함숫값으로 미분을 근사합니다. 전진 차분은 [f(x+h)−f(x)]/h, 후진 차분은 [f(x)−f(x−h)]/h입니다.

중심 차분 [f(x+h)−f(x−h)]/(2h)는 양쪽 정보를 사용해 같은 h에서 더 대칭적인 근사를 제공합니다. h가 작을수록 이론적 절단 오차는 줄지만 측정 잡음과 반올림 오차의 영향이 커질 수 있습니다.
