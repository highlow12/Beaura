# 찾는 길을 미리 정리하기

인덱스는 테이블의 특정 열 값을 별도 구조에 정리해 조건에 맞는 행의 위치를 빠르게 찾도록 돕습니다. B-tree 계열 인덱스는 균형 잡힌 트리의 정렬된 키를 사용해 등가 비교와 범위 검색을 일정한 높이에 가까운 비용으로 처리합니다.

인덱스가 있으면 DBMS가 전체 테이블 스캔 대신 인덱스를 따라 후보를 찾을 수 있지만, 모든 질의가 자동으로 빨라지는 것은 아닙니다. 선택도가 낮거나 결과 대부분을 읽어야 하면 테이블 스캔이 더 나을 수 있고, 표현식·형 변환 때문에 인덱스를 못 탈 수도 있습니다.
