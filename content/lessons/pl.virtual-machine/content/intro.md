# 바이트코드를 실행하는 기계

가상 머신(VM)은 소스 언어를 특정 CPU의 세부사항과 분리해 바이트코드 같은 중간 명령을 실행하는 프로그램입니다. 같은 바이트코드를 여러 운영체제에서 실행할 수 있지만 각 환경에 맞는 VM 구현이 필요합니다.

바이트코드는 네이티브 기계어보다 구조가 단순하거나 이식성이 높도록 설계할 수 있습니다. VM은 이를 해석하거나 실행 전에 기계어로 번역하며, 두 방식을 섞는 JIT도 사용할 수 있습니다.
