# 인터프리터 루프와 프레임

스택 기반 VM의 인터프리터 루프는 현재 프레임의 프로그램 카운터에서 명령을 가져오고(fetch), 해석한 뒤, 피연산자 스택을 조작하고 PC를 갱신합니다. 함수 호출은 새 프레임을 만들고 반환은 이전 프레임과 값을 복원합니다.

레지스터 기반 VM은 명령어가 스택 대신 가상 레지스터 번호를 직접 참조합니다. 스택 기반은 바이트코드가 간결할 수 있고, 레지스터 기반은 명령어 수와 데이터 이동을 줄일 수 있지만 어느 쪽이 항상 빠른 것은 아닙니다.
