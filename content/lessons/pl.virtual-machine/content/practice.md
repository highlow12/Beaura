## VM 실행 추적

명령어를 손으로 추적할 때는 PC, 피연산자 스택, 지역 프레임을 매 단계 표로 적으세요. `PUSH 2`, `PUSH 3`, `ADD`라면 ADD 직전 스택에는 2와 3이 있고, 실행 뒤에는 결과 5 하나만 남습니다.

VM 오류는 바이트코드의 잘못된 스택 깊이, 프레임 경계, 존재하지 않는 명령어처럼 나타날 수 있습니다. 바이트코드를 생성하는 컴파일러와 실행하는 VM이 같은 스택 효과와 호출 규약을 공유해야 이런 오류를 예방할 수 있습니다.
