# 닫힘 성질과 한계

정규 언어는 합집합, 연결, 클레이니 스타, 여집합과 교집합 같은 여러 연산에 대해 닫혀 있습니다. 즉 두 언어가 정규라면 이런 연산으로 만든 언어도 정규이며, 오토마톤을 조합하거나 변환해 이를 보일 수 있습니다.

그러나 모든 패턴이 정규인 것은 아닙니다. 임의로 같은 개수를 세어야 하는 `{0^n1^n | n ≥ 0}` 같은 언어는 유한한 상태만으로 필요한 개수를 기억할 수 없어 정규 언어가 아닙니다.
