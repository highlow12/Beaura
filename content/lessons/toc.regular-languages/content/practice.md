## 패턴과 기계 연결하기

`0(0|1)*`는 0으로 시작하는 모든 이진 문자열을 뜻합니다. 첫 기호를 확인한 뒤 나머지는 자유롭게 읽으면 되므로, 시작 상태에서 0을 읽은 뒤 수용 상태에 머무는 DFA를 만들 수 있습니다.

정규식을 읽을 때는 우선 연산자 범위를 괄호로 묶고, 빈 문자열과 빈 언어를 혼동하지 마세요. `ε`는 문자열 하나를 포함하지만 `∅`는 아무 문자열도 포함하지 않습니다.
