## 재귀 규칙 따라가기

`S → aSb | ε`는 같은 수의 `a`와 `b`가 순서대로 나타나는 문자열을 생성합니다. 규칙을 n번 재귀 적용하면 앞에 `a`가 n개, 뒤에 `b`가 n개 생기고, 마지막에 ε를 선택해 유도를 끝냅니다.

CFG 문제에서는 규칙을 적용할 비단말과 적용 횟수를 먼저 표시하세요. 오른쪽에 비단말이 여러 개 남으면 어느 것을 먼저 바꾸어도 최종 언어를 설명할 수 있지만, 유도 순서와 구문 트리는 구별해야 합니다.
