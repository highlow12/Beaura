## 선택 신호 따라가기

멀티플렉서의 후보 값은 **데이터 입력(data input)**, 어느 입력을 고를지 정하는 값은 **선택 신호(select)**, 선택된 값이 나오는 선은 **출력(output)**입니다.

2:1 멀티플렉서에서 입력이 D0=0, D1=1일 때 select=0이면 출력은 D0인 0이고, select=1이면 D1인 1입니다. 선택 신호는 데이터를 새로 계산하지 않고 여러 입력 중 하나가 출력으로 지나갈 경로만 정합니다.
