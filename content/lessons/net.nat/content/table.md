# 변환 테이블 추적

내부 장치 `192.168.0.10:53000`이 서버 `203.0.113.20:443`에 연결한다고 해 봅시다. 라우터는 외부에 `198.51.100.5:40001`로 보이도록 변환하고 다음 대응을 기억합니다.

`192.168.0.10:53000 ↔ 198.51.100.5:40001`

서버의 응답이 `198.51.100.5:40001`로 돌아오면 라우터는 테이블을 조회하여 목적지를 원래 내부 소켓으로 복원합니다. 공인 포트가 다르면 같은 공인 IP 하나로도 여러 연결을 구분할 수 있습니다.
