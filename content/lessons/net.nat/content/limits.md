# NAT의 경계

NAT는 사설 주소를 숨기고 공인 IPv4 주소 사용량을 줄이지만, 그 자체가 완전한 보안 장치는 아닙니다. 허용 규칙과 패킷 상태를 검사하는 방화벽의 역할과 구분해야 합니다.

외부에서 먼저 시작하는 연결은 대응하는 변환 항목이 없으므로 기본적으로 내부 장치에 도달하기 어렵습니다. 서버를 공개하려면 정적 NAT나 포트 포워딩처럼 명시적인 매핑이 필요합니다. NAT는 주소 고갈을 완화하지만 종단 간 연결성을 복잡하게 만든다는 대가가 있습니다.
