# DNS 이름 해석

사람은 `example.com` 같은 이름을 기억하지만 패킷은 목적지 IP 주소가 필요하므로 이름을 주소로 변환하는 시스템이 필요합니다.

DNS는 도메인 이름과 레코드를 계층적으로 관리하는 분산 이름 시스템입니다. 리졸버는 캐시를 먼저 확인하고 필요하면 루트·TLD·권한 서버를 따라 질의합니다.

이 레슨에서는 패킷이 어느 계층에서 어떤 주소와 상태를 얻는지 따라가며 네트워크 용어를 구분합니다.
