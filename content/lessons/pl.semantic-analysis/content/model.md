# 심벌 테이블과 스코프

심벌 테이블은 이름을 선언 정보에 매핑합니다. 함수나 블록에 들어가면 새 테이블을 만들거나 부모를 가리키고, 이름을 조회할 때 현재 스코프부터 바깥으로 검색합니다. 같은 스코프의 중복 선언과 바깥 이름 가리기는 언어 규칙에 따라 검사합니다.

타입 검사에서는 각 AST 노드가 기대 타입을 만들어 부모로 전달합니다. 함수 호출은 함수 타입의 인자 수와 타입을 비교하고, 변수 대입은 대상이 허용하는 타입과 값의 타입을 비교합니다.
