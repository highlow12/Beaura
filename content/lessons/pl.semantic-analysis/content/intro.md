# 구문 다음의 검사

파서가 AST를 만든 뒤에도 이름이 선언되었는지, 연산 타입이 맞는지, 반환문이 함수 계약을 지키는지 확인해야 합니다. 이 단계가 의미 분석(semantic analysis)이며 프로그램의 실행 전에 발견할 수 있는 제약을 모읍니다.

의미 분석기는 AST를 순회하면서 각 노드에 타입과 심벌 정보를 주석처럼 붙입니다. 이후 코드 생성기는 이미 해석된 이름과 타입을 사용하므로 같은 검색과 판단을 반복하지 않아도 됩니다.
