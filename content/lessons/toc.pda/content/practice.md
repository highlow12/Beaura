## `a^n b^n` 추적

입력 `aaabbb`를 처리하는 PDA는 앞의 세 `a`를 읽으며 스택에 기호를 세 번 push합니다. 뒤의 `b`마다 하나씩 pop하면 입력이 끝날 때 스택이 시작 표식만 남아 두 개수가 같음을 확인할 수 있습니다.

반대로 `aaabb`처럼 pop이 부족하거나 `aabbb`처럼 스택이 먼저 비면 거부합니다. PDA를 손으로 풀 때는 현재 상태·남은 입력·스택 내용을 한 줄씩 써서 입력을 소비하지 않는 ε 전이와 실제 입력 전이를 구분하세요.
