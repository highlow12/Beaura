# 구성과 정지

튜링 기계의 전이 함수는 `(상태, 읽은 기호) → (새 상태, 쓸 기호, 이동 방향)` 꼴입니다. 전이가 정의되지 않은 상태에 도달하거나 명시적인 accept·reject 상태에 들어가면 실행이 멈춥니다.

한 단계는 기호 하나를 읽고 쓰고 이동하는 동작입니다. 같은 기계라도 초기 테이프와 헤드 위치가 바뀌면 실행이 달라지므로, 시뮬레이션에서는 테이프의 빈칸 기호와 현재 헤드 칸을 함께 표시해야 합니다.
