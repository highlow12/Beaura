# 도달 가능한 객체

가비지 컬렉션(GC)은 프로그램이 더 이상 접근할 수 없는 힙 객체를 자동으로 회수합니다. 스택, 전역 변수, 레지스터처럼 실행 중 직접 접근할 수 있는 위치를 루트(root)로 보고 참조를 따라 도달 가능한 객체를 찾습니다.

참조가 끊겼다는 것은 객체가 즉시 삭제된다는 뜻이 아니라 다음 수집 시점에 회수 후보가 된다는 뜻입니다. 루트에서 우연히 연결된 캐시나 전역 목록이 있으면 사용하지 않는 것처럼 보여도 계속 살아 있습니다.
