## 제어 신호로 연산 선택하기

ALU가 계산에 쓰는 입력 값을 **피연산자(operand)**, 수행할 연산을 고르는 값을 **제어 신호(control signal)**, 결과의 조건을 요약하는 상태 비트를 **플래그(flag)**라고 합니다.

피연산자가 5와 5이고 제어 신호가 SUB라면 결과는 0이며 zero 플래그는 1이 될 수 있습니다. 제어 신호가 AND라면 같은 비트 입력에도 산술 뺄셈이 아니라 비트별 AND 결과가 나옵니다. ALU 결과가 어디에 기록될지는 별도의 제어가 결정합니다.
