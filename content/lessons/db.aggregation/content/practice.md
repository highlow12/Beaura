## 집계에서 NULL 읽기

`COUNT(*)`는 행 수를 세지만 `COUNT(column)`은 해당 column이 NULL이 아닌 행만 셉니다. `AVG`와 `SUM`도 DBMS의 NULL 규칙에 따라 값이 없는 행을 보통 제외합니다. 따라서 “행의 수”와 “값이 존재하는 행의 수”를 구분해 함수와 조건을 선택해야 합니다.

GROUP BY에 없는 일반 열을 SELECT에 섞으면 어떤 행의 값을 보여 줘야 하는지 모호해집니다. 집계 결과에 필요한 열은 그룹 키이거나 집계 함수 안에 있어야 한다는 원칙을 먼저 확인하세요.
