# TCP 연결과 신뢰성

손실과 순서 변경이 있는 IP 위에서 파일이나 웹 요청을 정확히 이어 보내려면 전송 계층이 상태와 복구 절차를 관리해야 합니다.

TCP는 연결 상태를 유지하는 바이트 스트림 프로토콜입니다. 시퀀스 번호, 확인 응답, 재전송, 흐름·혼잡 제어로 수신자가 순서 있는 데이터를 받도록 돕습니다.

이 레슨에서는 패킷이 어느 계층에서 어떤 주소와 상태를 얻는지 따라가며 네트워크 용어를 구분합니다.
