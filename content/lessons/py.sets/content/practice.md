## 중복 제거에 활용하기

리스트에서 중복을 제거하고 싶다면 unique = set(values)처럼 집합으로 바꿀 수 있습니다. 다시 리스트가 필요하면 list(unique)로 변환하지만, 원래 순서가 보존되어야 하는 문제에는 별도 전략이 필요합니다.

자료구조를 고를 때 중복 허용 여부와 순서 필요 여부를 함께 확인하면 set과 list를 올바르게 구분할 수 있습니다.
