# 상수 접기와 죽은 코드 제거

상수 접기(constant folding)는 컴파일 시점에 계산할 수 있는 `2 * 3`을 `6`으로 바꾸는 변환입니다. 상수 전파는 `x = 6`이라는 사실을 이후 사용 위치에 전달해 더 많은 계산을 접을 수 있게 합니다.

죽은 코드 제거(dead-code elimination)는 결과나 부수 효과에 영향을 주지 않는 명령을 삭제합니다. `if False`의 몸체처럼 도달할 수 없는 부분과, 계산했지만 읽지 않는 임시값은 분석 결과가 충분할 때 제거 대상이 됩니다.
