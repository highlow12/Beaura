# 더 짧지만 같은 프로그램

컴파일러 최적화는 관찰 가능한 의미를 유지하면서 실행 시간, 메모리, 코드 크기 같은 비용을 줄이는 변환입니다. 최적화 전후의 AST나 IR이 달라도 같은 입력·환경에서 같은 결과와 부수 효과를 내야 합니다.

최적화는 분석과 변환의 쌍으로 이루어집니다. 어떤 값이 상수인지, 어떤 명령이 도달 가능한지, 어떤 메모리가 서로 영향을 주는지 분석한 뒤 그 사실이 보장하는 범위 안에서만 코드를 바꿉니다.
