# 값에 붙는 타입

타입은 값의 종류와 허용되는 연산을 설명하는 분류입니다. 정수와 문자열을 구분하면 덧셈의 의미를 결정할 수 있고, 함수 타입을 사용하면 호출할 수 있는 인자와 반환값의 모양을 기록할 수 있습니다.

정적 타입 언어는 보통 실행 전에 타입을 검사하고, 동적 타입 언어는 실행 중 값에 붙은 타입을 확인합니다. “정적”은 타입이 절대 바뀌지 않는다는 뜻이 아니라 검사를 수행하는 시점에 관한 말입니다.
