# 파이프라인과 해저드

명령어 하나가 끝날 때까지 다음 명령어를 기다리면 CPU의 여러 회로가 놀게 됩니다.

파이프라인은 fetch, decode, execute 같은 단계를 나누고 서로 다른 명령어를 각 단계에서 동시에 처리합니다. 처리량은 높아지지만 한 명령어의 전체 지연이 반드시 줄어드는 것은 아닙니다.

이 레슨의 목표는 용어를 외우는 데 그치지 않고, 입력이 어떻게 표현되고 어떤 하드웨어 경로를 거쳐 결과가 되는지 설명하는 것입니다.
