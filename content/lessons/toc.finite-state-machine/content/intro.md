# 상태로 기억하는 계산

유한 상태 기계(FSM)는 현재 상태, 입력 기호, 다음 상태를 연결하는 전이 규칙으로 계산을 표현합니다. 시작 상태에서 입력을 한 기호씩 읽고 전이를 따라가면 입력이 끝났을 때의 상태가 계산 결과가 됩니다.

상태는 과거 입력 전체를 저장하지 않고 미래 판단에 필요한 요약만 저장합니다. 예를 들어 지금까지 읽은 `1`의 개수가 홀수인지 짝수인지만 필요하다면 두 상태만으로 충분합니다.
