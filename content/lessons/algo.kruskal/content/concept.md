## Union-Find로 사이클 검사

Union-Find는 각 정점의 대표를 find로 찾고, 서로 다른 대표를 가진 두 묶음을 union으로 합칩니다. 두 끝점의 대표가 같다면 이미 경로가 있어 그 간선을 추가하면 사이클이 됩니다.

간선 정렬에 O(E log E), Union-Find 연산에 거의 상수에 가까운 비용이 들어 기본 시간 복잡도는 O(E log E)입니다.
