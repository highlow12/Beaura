# 콘텐츠 확장 로드맵

최종 갱신: 2026-09-15

이 문서는 Beaura 베타의 콘텐츠 확장 결과와 다음 보강 방향을 기록한다. 초기 대량 확장은 완료됐으며, 현재의 YAML/Markdown 콘텐츠 구조와 영구 lesson/question ID 정책을 따른다.

## 현재 상태

- 활성 트랙 15개, 레슨 236개, 문제 1,190개.
- 모든 레슨은 설명 블록 3개 이상, 문제 5개 이상이라는 CI 품질 기준을 충족한다.
- 네트워크와 컴퓨터 그래픽스는 독립 트랙으로 분리됐다.
- `computer-systems`는 영구 ID 호환을 위해 레슨 없는 legacy track으로만 보존한다.
- 다음 단계는 레슨 수 확대보다 기술적 정확성, 설명-문제 정렬, 오답 선택지 품질, 난이도 곡선의 수동 검토다.

## 확장 원칙

- 하나의 레슨은 하나의 핵심 학습 목표만 다룬다.
- 레슨 하나는 설명 블록 3 ~ 4개와 문제 5 ~ 7개를 목표로 한다.
- 정의 암기보다 계산, 추적, 비교, 순서 배열 문제를 우선한다.
- 기존 lesson ID와 question ID는 삭제하거나 재사용하지 않는다.
- 기존 개요 레슨은 유지하고, 새 레슨을 사이에 삽입한다.
- 새 문제 유형이 꼭 필요하지 않다면 현재 문제 템플릿을 우선 활용한다.
- 수학은 CS 응용과 연결하되, 수학 과목을 모두 끝내야 다른 트랙이 열리는 구조는 피한다.

## 적용된 트랙 구조

기존의 `computer-systems` 응용 트랙은 다음 두 트랙으로 분리했다.

- `networks`: 컴퓨터 네트워크
- `computer-graphics`: 컴퓨터 그래픽스

`computer-systems` track ID는 기존 데이터 호환을 위해 남겨 두되 소속 레슨이 없어 학습 화면에는 표시하지 않는다.

수학은 하나의 긴 순서가 아니라 서로 연결되는 네 개의 병렬 트랙으로 구성한다.

- `discrete-math`: 이산수학
- `linear-algebra`: 선형대수
- `calculus`: 미적분학
- `probability-statistics`: 확률과 통계

## 컴퓨터 구조

초기 구성:

`arch.binary → arch.logic → arch.cpu`

확장 기준:

| 순서 | 레슨               | 핵심 내용                              |
| ---: | ------------------ | -------------------------------------- |
|    1 | 이진수와 비트      | 자리값, 비트, 바이트, 16진수           |
|    2 | 정수의 표현        | unsigned, 2의 보수, 음수, 오버플로     |
|    3 | 데이터의 표현      | 문자, 명령어, 실수의 비트 표현         |
|    4 | 논리 게이트        | AND, OR, NOT, XOR, 진리표              |
|    5 | 조합 논리회로      | 반가산기, 전가산기, 멀티플렉서, 디코더 |
|    6 | 순차 논리회로      | 상태, 클록, 래치, 플립플롭             |
|    7 | 레지스터와 메모리  | 비트 저장, 주소, 읽기와 쓰기           |
|    8 | 명령어 집합        | opcode, operand, 기계어, 주소 지정     |
|    9 | CPU 데이터패스     | PC, IR, 레지스터, ALU, 제어 장치       |
|   10 | CPU 실행 사이클    | fetch, decode, execute, store 추적     |
|   11 | 메모리 계층과 캐시 | 레지스터, 캐시, RAM, 지역성            |
|   12 | 입출력과 인터럽트  | I/O, 인터럽트, DMA                     |
|   13 | 파이프라이닝       | 단계 중첩, hazard, 분기                |
|   14 | 성능과 병렬성      | 클록, CPI, 멀티코어, SIMD              |

기존 `arch.binary`, `arch.logic`, `arch.cpu` ID를 유지하면서 이 범위를 넘어 파이프라이닝까지 확장했다.

## 수학

### 이산수학

명제와 논리 → 집합 → 관계와 함수 → 증명법 → 수학적 귀납법 → 경우의 수 → 점화식 → 그래프 이론 → 정수론과 모듈러 연산

주요 연결:

- 자료구조와 알고리즘
- 논리회로와 불 대수
- 그래프 탐색과 라우팅
- 암호학

### 선형대수

벡터 → 행렬 연산 → 연립방정식과 가우스 소거법 → 선형결합·생성·독립 → 기저와 차원 → 선형변환 → 행렬식과 랭크 → 고유값·고유벡터 → 직교와 투영

주요 연결:

- 2D·3D 그래픽스
- 좌표 변환과 카메라
- 머신러닝
- 물리 시뮬레이션

### 미적분학

함수와 그래프 → 극한과 연속 → 미분 → 미분의 활용 → 적분 → 적분의 활용 → 편미분 → 그래디언트 → 최적화와 수치 계산

주요 연결:

- 최적화
- 물리 시뮬레이션
- 고급 그래픽스
- 머신러닝

### 확률과 통계

표본공간과 사건 → 확률 법칙 → 조건부확률 → 베이즈 정리 → 확률변수 → 기댓값과 분산 → 이산·연속분포 → 표본과 추정 → 신뢰구간과 가설검정

주요 연결:

- 알고리즘 분석
- 네트워크 성능
- 데이터 과학
- 머신러닝

초기 계획은 과목당 4개 레슨이었으며, 현재는 다음 규모로 확장됐다.

- 이산수학 18개 레슨
- 선형대수 12개 레슨
- 미적분 10개 레슨
- 확률과 통계 12개 레슨

## 컴퓨터 네트워크

기존 `sys.network`는 `networks` 트랙의 개요 레슨으로 유지한다. 영구 ID 정책에 따라 ID의 `sys.` 접두사는 변경하지 않는다.

| 순서 | 레슨               | 핵심 내용                             |
| ---: | ------------------ | ------------------------------------- |
|    1 | 네트워크 기초      | 장치, 링크, 패킷, 프로토콜            |
|    2 | 전송 성능          | 대역폭, 지연, 처리량, 손실            |
|    3 | 계층과 캡슐화      | TCP/IP 계층, 헤더, 캡슐화             |
|    4 | 이더넷과 MAC       | 프레임, MAC 주소, 스위치              |
|    5 | IP 주소            | IPv4, 네트워크·호스트 부분            |
|    6 | 서브넷             | 서브넷 마스크, CIDR, 주소 계산        |
|    7 | 라우터와 전달      | 게이트웨이, 라우팅 테이블, hop        |
|    8 | ARP와 ICMP         | IP-MAC 연결, ping, 오류               |
|    9 | 포트와 소켓        | 프로세스와 통신 끝점                  |
|   10 | UDP                | 데이터그램, 손실 허용, 낮은 지연      |
|   11 | TCP 신뢰성         | 연결, 순서 번호, ACK, 재전송          |
|   12 | TCP 흐름·혼잡 제어 | 윈도, 수신자 보호, 혼잡 대응          |
|   13 | DNS                | 이름 해석, 질의, 캐시                 |
|   14 | HTTP               | 요청, 응답, 메서드, 상태 코드         |
|   15 | TLS와 HTTPS        | 인증서, 암호화, 핸드셰이크            |
|   16 | NAT와 포트 변환    | 사설 주소, 공인 주소, 변환 테이블     |
|   17 | 네트워크 진단      | ping, traceroute, nslookup, 패킷 추적 |

현재 네트워크 트랙은 개요부터 NAT, DNS, HTTP, TLS까지 18개 레슨으로 확장됐다.

핵심 문제:

- 캡슐화 순서 배열
- 서브넷 주소와 호스트 범위 계산
- 라우팅 테이블에서 다음 hop 선택
- TCP 순서 번호와 ACK 추적
- TCP와 UDP 사용 사례 비교
- 장애가 발생한 계층 추론

## 컴퓨터 그래픽스

기존 `sys.graphics`는 `computer-graphics` 트랙의 개요 레슨으로 유지한다. 영구 ID 정책에 따라 ID의 `sys.` 접두사는 변경하지 않는다.

| 순서 | 레슨              | 핵심 내용                        |
| ---: | ----------------- | -------------------------------- |
|    1 | 그래픽스 기초     | 픽셀, 정점, 프레임버퍼, 렌더링   |
|    2 | 디지털 색상       | RGB, 알파, 색 깊이               |
|    3 | 이미지 표현       | 해상도, 래스터·벡터, 압축        |
|    4 | 벡터와 좌표       | 위치, 방향, 내적, 좌표계         |
|    5 | 행렬 변환         | 이동, 회전, 크기, 동차좌표       |
|    6 | 메시와 삼각형     | 정점, 간선, 면, 인덱스           |
|    7 | 좌표 공간         | 모델, 월드, 뷰, 클립 공간        |
|    8 | 카메라와 투영     | 뷰 행렬, 원근·직교 투영          |
|    9 | 클리핑과 뷰포트   | 가시 영역, NDC, 화면 좌표        |
|   10 | 래스터화          | 삼각형에서 픽셀 후보 생성        |
|   11 | 보간              | 무게중심 좌표, UV·색상·깊이      |
|   12 | 깊이와 가시성     | 깊이 버퍼, 뒷면 제거             |
|   13 | 셰이더            | 버텍스·프래그먼트 셰이더         |
|   14 | 텍스처            | UV, 필터링, 래핑, 밉맵           |
|   15 | 조명과 재질       | 노멀, 내적, 확산광, 정반사       |
|   16 | 투명도와 블렌딩   | 알파, 그리기 순서                |
|   17 | 그림자            | 섀도 맵의 원리와 한계            |
|   18 | GPU 구조          | 병렬 처리, 워프·웨이브           |
|   19 | 렌더링 성능       | 드로 콜, 배칭, 메모리 대역폭     |
|   20 | 레이 트레이싱     | 광선, 교차, 반사, 그림자         |
|   21 | 애니메이션과 보간 | 키프레임, 보간, 변환의 시간 변화 |

현재 컴퓨터 그래픽스 트랙은 개요부터 애니메이션까지 17개 레슨으로 확장됐다.

핵심 문제:

- 변환 후 정점 좌표 계산
- 좌표 공간 변환 순서 배열
- 삼각형의 앞뒤와 깊이 판정
- 셰이더 단계 역할 연결
- UV 좌표와 텍스처 위치 계산
- 노멀과 광원 방향으로 밝기 비교
- 투명 오브젝트의 렌더링 순서 선택

## 트랙 간 연결 정책

- 컴퓨터 구조의 기존 CPU 이후에 네트워크와 그래픽스 개요를 둔다.
- 네트워크 입문을 수학 트랙 완료 조건으로 만들지 않는다.
- 그래픽스 입문을 선형대수 완료 조건으로 만들지 않는다.
- 그래픽스의 변환·조명 심화 레슨에서 선형대수 레슨을 권장 선행으로 연결한다.
- 라우팅 알고리즘 심화에서 이산수학의 그래프 이론을 연결한다.
- 연속확률은 미적분의 적분 이후에 배치한다.
- 고급 그래픽스와 최적화는 선형대수·미적분의 관련 레슨 이후에 배치한다.

## 다음 작업

1. 트랙별 표본 레슨을 실제 초보 학습 흐름으로 수동 검토한다.
2. 문제에서 요구하지만 앞선 설명에 없는 개념을 보강한다.
3. 정답을 몰라도 문장 길이나 표현만으로 고를 수 있는 선택지를 교정한다.
4. 트랙 안팎의 선행 관계와 난이도 상승 폭을 실제 플레이테스트로 조정한다.
5. 기술적 정확성 검토 결과를 lesson revision과 함께 반영한다.

현재 9개 문제 유형을 지원한다. 행렬·벡터 계산, 패킷 상태 추적, 좌표 변환, 논리회로 출력처럼 별도 상호작용이 필요하지 않은 문제는 기존 유형과 콘텐츠 블록을 조합한다.
